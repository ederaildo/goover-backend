'use strict';

// Configuring the Categorias module
/*angular.module('categorias').run(['Menus',
	function(Menus) {
		// Set top bar menu items
		Menus.addMenuItem('topbar',{
			title: 'Categorias', 
			state: 'categorias',
			type: 'dropdown', 
			roles: ['admin']
		});
		Menus.addSubMenuItem('topbar', 'categorias', {
			title: 'List Categorias',
			state: 'categorias.list',
			roles: ['admin']
		});
		Menus.addSubMenuItem('topbar', 'categorias', {
			title: 'New Categoria',
			state: 'categorias.create',
			roles: ['admin']
		});
	}
]);*/
